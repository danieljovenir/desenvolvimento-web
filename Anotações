cd docker
docker compose up -d -> subir o container com o postgres
docker compose stop -> parar o container do postgres

-- public.usuarios definition

-- Drop table

-- DROP TABLE public.usuarios;

CREATE TABLE public.usuarios (
	id serial4 NOT NULL,
	nome varchar(255) NOT NULL,
	email varchar(255) NOT NULL,
	senha varchar(255) NOT NULL,
	data_nascimento varchar(255) NOT NULL,
	telefone varchar(255) NOT NULL,
	CONSTRAINT usuarios_pkey PRIMARY KEY (id)
);

docker network create lar_das_meninas 

-- toda vez que realizar uma alteração, é preciso escrever o comando abaixo para add as alterações no repository
git add .

-- realizar comentario nas alterações
git commit -m 'adicionei tal coisa'

-- serve para empurrar as informações para o git
git push